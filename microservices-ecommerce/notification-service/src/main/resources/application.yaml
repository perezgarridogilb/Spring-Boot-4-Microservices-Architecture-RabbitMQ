spring:
  application:
    name: notification-service
  config:
    import: optional:configserver:http://localhost:8888
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 587
    username: aa1032f4896968
    password: 1930fd4ce0fbef
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 0

logging:
  level:
    root: INFO
    com.ecommerce: INFO
